{
  "name": "remon-doc",
  "version": "0.1.0",
  "description": "RemoteMonster Documents",
  "main": "README.md",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "gitbook install",
    "serve": "gitbook serve",
    "build": "npm run clean && gitbook build",
    "clean": "rm -rf _book",
    "publish": "npm run build && gh-pages --dist _book"
  },
  "author": "RemoteMonster",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "gitbook-cli": "^2.3.0"
  }
}
