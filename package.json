{
  "name": "remon-doc",
  "version": "0.1.0",
  "description": "RemoteMonster Documents",
  "main": "README.md",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "gitbook install",
    "start": "gitbook serve",
    "build": "npm run clean && gitbook build",
    "clean": "rm -rf _book",
    "deploy": "npm run build && gh-pages --dist _book"
  },
  "author": "RemoteMonster",
  "license": "UNLICENSED",
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "gitbook-cli": "^2.3.0"
  },
  "dependencies": {
    "gitbook-plugin-edit-link": "^2.0.2",
    "gitbook-plugin-ga": "^1.0.1",
    "gitbook-plugin-github": "^2.0.0",
    "gitbook-plugin-language-picker": "^0.1.0",
    "gitbook-plugin-sitemap": "^1.2.0",
    "gitbook-plugin-theme-api": "^1.1.2",
    "gitbook-plugin-versions": "^2.1.4"
  }
}
